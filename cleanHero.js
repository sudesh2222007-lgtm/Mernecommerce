const fs = require(" fs\); const p = \frontend/src/pages/Home.jsx\; let lines = fs.readFileSync(p, \utf8\).split(\\n\); let start = -1, end = -1; for(let i=0; i<lines.length; i++){ if(lines[i].indexOf(\Left Column\) !== -1) start = i; if(lines[i].indexOf(\Right Column\) !== -1) end = i; } if(start !== -1 && end !== -1){ lines.splice(start, end - start); fs.writeFileSync(p, lines.join(\\n\), \utf8\); console.log(\Hero text removed successfully!\); }